# Sanity CMS — copy to .env and fill in your values
SANITY_PROJECT_ID=your_project_id_here
SANITY_DATASET=production
SANITY_TOKEN=your_read_token_here   # only needed for private datasets

# Vite base path — set to / if using a custom domain
VITE_BASE=/pages-test/
